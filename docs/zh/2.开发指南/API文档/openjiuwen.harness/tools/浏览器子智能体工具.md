# 浏览器子智能体工具

浏览器全部工具、Rails、运行时与生命周期已统一到[浏览器子智能体](../subagents/浏览器子智能体.md)。
本页保留旧章节入口，不再维护第二套清单和默认值。
核对日期：2026-09-21；适用范围包括本地尚未提交的实现，不能直接套用到旧 SDK/EXE。

## Playwright MCP 工具

完整列表和注册规则见[完整工具清单](../subagents/浏览器子智能体.md#完整工具清单)。
`browser_capabilities=None` / `[]` 只启用 core，不是全部 MCP 工具。

### core（始终包含）

见[core](../subagents/浏览器子智能体.md#core始终包含)。截图、代码执行、网络诊断、窗口调整和拖放不属于 core；`browser_wait_for` 不在当前受信目录内。

### pdf

见[pdf / vision](../subagents/浏览器子智能体.md#pdf--vision)。截图还需 agent 实际支持图像输入。

### vision

见[pdf / vision](../subagents/浏览器子智能体.md#pdf--vision)。截图还需 agent 实际支持图像输入。

### devtools

见[devtools](../subagents/浏览器子智能体.md#devtools)。

### config

见[config / network](../subagents/浏览器子智能体.md#config--network)。

### network

见[config / network](../subagents/浏览器子智能体.md#config--network)。

### storage

见[storage](../subagents/浏览器子智能体.md#storage)。

### testing

见[testing](../subagents/浏览器子智能体.md#testing)。

## 运行时辅助工具

默认仅两类 Probe、Batch，以及工厂另行添加的 offload recall。
取消、健康检查和 custom-action Tool 类需要显式装配，不是 capability 自动注入项。

### 页面探测

当前 Probe 使用 PageState target，不再要求模型拼造 selector。

#### tool browser_probe_interactives

见[browser_probe_interactives](../subagents/浏览器子智能体.md#tool-browser_probe_interactives)。

#### tool browser_probe_cards

见[browser_probe_cards](../subagents/浏览器子智能体.md#tool-browser_probe_cards)。

### tool browser_batch_interact

见[browser_batch_interact](../subagents/浏览器子智能体.md#tool-browser_batch_interact)。

### tool browser_recall_offload

见[browser_recall_offload](../subagents/浏览器子智能体.md#tool-browser_recall_offload)。

### tool browser_custom_action

见[管理与兼容工具](../subagents/浏览器子智能体.md#管理与兼容工具非默认装配)。

### tool browser_list_custom_actions

见[管理与兼容工具](../subagents/浏览器子智能体.md#管理与兼容工具非默认装配)。

### tool browser_cancel_run

见[管理与兼容工具](../subagents/浏览器子智能体.md#管理与兼容工具非默认装配)。

### tool browser_clear_cancel

见[管理与兼容工具](../subagents/浏览器子智能体.md#管理与兼容工具非默认装配)。

### tool browser_runtime_health

见[管理与兼容工具](../subagents/浏览器子智能体.md#管理与兼容工具非默认装配)。

## 取消与排障

清除取消标志不等于恢复 Agent query；详见[取消与人工接手](../subagents/浏览器子智能体.md#取消与人工接手)和[排障](../subagents/浏览器子智能体.md#可观测性与排障)。
